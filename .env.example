# === 必填 ===
CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
USER_ID=your_line_user_id_here

# === 選填（不填就用預設值）===
# 檢查間隔，使用 cron 語法（預設：每 5 分鐘）
# CHECK_INTERVAL=*/5 * * * *

# 刊登數門檻，低於此數的賽事不通知（預設：1）
# MIN_LISTINGS=1

# HTTP 請求逾時毫秒數（預設：15000）
# AXIOS_TIMEOUT=15000
